/////////////////////////////// app generic CSS /////////////////////////////////////

/////////////////////////////// page CSS /////////////////////////////////////

@media (min-width: 841px) {
  #main-view {
    display: none
  }
} 
@media (max-width: 840px) {
  #screen-too-large {
    display: none
  }
}
#q-app { 
  min-height: 0px; 
}
.wrapper {
  overflow-y: scroll;
}
.layout-page, .layout-page-container {
  overflow-y: hidden;
  height: 100%;
}
#main-view { 
  padding: 0rem; 
  height: inherit; 
  min-height: inherit;
}  
#main-view > div.wrapper {
  height: inherit; 
  min-height: inherit; 
  padding: 1rem; 
  display: flex; 
  flex-flow: column nowrap;
}  
.dark-background {
  background: #e82a34;
  color: #fff;
  min-height: 100%;
}
.grow {
  flex-grow: 1;
}
.dark-background a {
  color: #fff;
}
#main-view {
  padding: 0rem;
}
.infoWindow {
  text-align: center;
  padding: 3px;
}
.column {
  display: flex;
  flex-flow: column nowrap;
  /* MP 2018-10-12 causes problem with <q-tab> on Quasar 0.17, icons get aligned to left instead of being centered */
  /*align-items: stretch;*/
}
/* MP 2018-10-12 does not seem to be used, maybe remove in a few weeks */
/*.column .row {
  flex: 1;
}
.column .row, .column .row-auto {
  width: 100%;
}*/
.header {
  width: 100%;
  background-color: #ccc;
  color: #e82a34;
  min-height: 80px;
  font-size: 1.3rem;
  line-height: 1.1rem;
  background-repeat: no-repeat;
  padding: 10px;
}
.header .title {
  font-size: 1.5em;
  line-height: 1.1em;
}
.no-padding {
  padding: 0 !important;
}

/////////////////////////////// Links CSS /////////////////////////////////////

a {
  text-decoration: underline;
}
.no-underline {
  text-decoration: none;
}

/////////////////////////////// Panels CSS /////////////////////////////////////
/* MP 2018-11-12 for quasar 0.17, a width parameter has to be set directly on <q-layout-drawer> tag */
/*
.q-layout-drawer-left {
    width: 100%;
}
.q-layout-drawer-right {
    width: 100%;
}
*/
/////////////////////////////// Labels CSS /////////////////////////////////////
h1.size-1 {
  font-size: 3.5em;
}
h1.size-2 {
  font-size: 3em;
}
h1.size-3 {
  font-size: 2rem;
  font-weight: normal;
}
h2.size-1 {
  font-size: 2.6em;
}
h2.size-2 {
  font-size: 2.1em;
}
h2.size-3 {
  font-size: 1.8em;
}
h3.size-1 {
  font-size: 1.8em;
}
h3.size-2 {
  font-size: 1.5em;
}
.size-1 {
  font-size: 2em;
}
.align-right {
  text-align: right;
}
.infoWindow .title {
  font-size: 1.5em;
}
.centered {
  text-align: center;
}
.right {
  text-align: right;
}
span .q-icon {
  margin-top: -2px;
}

/////////////////////////////// Forms CSS /////////////////////////////////////

.dark-background .q-if-label {
  color: #fff;
}
.q-field {
  margin: 0 0 5vw 0;
}
.q-field-dark .q-field-label, .q-field-dark .q-field-icon, .q-field-dark .q-field-counter, .q-field-dark .q-field-bottom {
  color: #ef3;
}
.dark-background .q-if-focused .q-if-label {
  color: #eee !important;
}
form {
  margin-top: 1rem;
}
.q-btn {
  margin: 0.3rem 0; 
}
.dark-background .q-if:after {
  background-color: #fff;
}
.dark-background .q-input-target, .dark-background .q-input-shadow {
  color: #fff;
}
.dark-background .q-if-label, .dark-background .q-if-addon, .dark-background .q-if-control {
  color: #fff;
}
.dark-background .q-if-error:before, .dark-background .q-if-error:after, .dark-background .q-if-error:not(.q-if-inverted) .q-if-label {
  color: #fff;
}
.location-address { 
  display: flex; 
  flex-flow: row nowrap; 
  justify-content: center; 
  align-items: center; 
}
.location-address .q-input { 
  flex-grow: 1 
}
.location-address img { 
  width: 2.5rem; 
  height: 2.5rem;
}

/////////////////////////////// Buttons CSS /////////////////////////////////////

.multiple-btn button {
  margin-left: 20px
}
.multiple-btn button:first-child {
  margin-left: 0px
}

/////////////////////////////// Menus CSS /////////////////////////////////////

.menu {
  width: 100%;
  height: 120px;
  z-index: 5000;
  background-color: rgba(0, 0, 0, .2)
}
.menu>div {
  padding-top: 20px;
}
.menu>div:first-child, .menu>div:last-child {
  padding-top: 35px;
}
.menu-background {
  background: #000;
  opacity: 0.2;
}
.over-map {
  z-index: 6000;
}

/////////////////////////////// Tabs CSS /////////////////////////////////////

.q-tab-pane {
  padding-bottom: 100px; /* to avoid bottom information to be hidden by menu */
  border: 0px;
}
.step-menu .q-tabs-normal .q-tab-icon, .step-menu .q-tabs-normal .q-tab-label {
  opacity: 0.9 !important;
}
.disabled, [disabled] {
  opacity: 0.9 !important;
}
.step-menu .disabled, .step-menu [disabled] {
  opacity: 0 !important;
}
.step-menu {
  position: fixed !important;
}

/////////////////////////////// Images CSS /////////////////////////////////////

.preview {
  height: 200px;
}
.big-icon {
  font-size: 5rem
}
.medium-icon {
  font-size: 2.5rem
}

/////////////////////////////// Panel CSS /////////////////////////////////////

.panel-bottom {
  background: white; 
  position: fixed; 
  bottom: 0; 
  left: 0; 
  top: 0;
  width: 100%; 
  height: 100%; 
  overflow-y: scroll;
  z-index: 100;
}
.panel-bottom h1 {
  margin-top: 10px;
  margin-bottom: 20px;
}

/////////////////////////////// Animation CSS /////////////////////////////////////

.slideInBottom-enter-active, .slideInBottom-leave-active {
  transition: all .3s ease;
}
.slideInBottom-enter, .slideInBottom-leave-to {
  transform: translateY(100vh);
  opacity: 0;
}

/* display the controls progressively */
.fadeIn {
  animation: fadein 2s;
  -moz-animation: fadein 2s; /* Firefox */
  -webkit-animation: fadein 2s; /* Safari et Chrome */
  -o-animation: fadein 2s; /* Opera */
}
@keyframes fadein {
  from {
      opacity:0;
  }
  to {
      opacity:1;
  }
}
@-moz-keyframes fadein { /* Firefox */
  from {
      opacity:0;
  }
  to {
      opacity:1;
  }
}
@-webkit-keyframes fadein { /* Chrome */
  from {
      opacity:0;
  }
  to {
      opacity:1;
  }
}
@-o-keyframes fadein { /* Opera */
  from {
      opacity:0;
  }
  to {
      opacity: 1;
  }
}
.slideInBottom-enter-active, .slideInBottom-leave-active {
  transition: all .3s ease;
}
.slideInBottom-enter, .slideInBottom-leave-to {
  transform: translateY(100vh);
  opacity: 0;
}

.fadein-message {
  position: absolute;
  z-index: 0;
  top: 30%;
  width: 100%;
  height: 0;
  overflow: hidden;
  margin: auto;
  text-align: center;
  font-size: 88px;
  color: #FFF;
  transform-origin: 50% 100%;
  text-shadow: 2px 2px 2px #333;
  animation-duration: 2s;
  animation-name: fadeAndScale;
  animation-fill-mode: forwards;
}

@keyframes fadeAndScale {
  from {
      opacity: 0;
      transform: scale(.9, .9);
      z-index: 10000;
      height: auto;
  }
  30% {
      opacity: 1;
      transform: scale(1, 1);
      height: auto;
  }
  70% {
      opacity: 1;
      transform: scale(1, 1);
      height: auto;
  }
  99% {
      opacity: 0;
      transform: scale(1.1, 1.1);
      height: auto;
  }
  to {
      height: 0;
      opacity: 0;
  }
}

@keyframes flipInY {
  from {
    transform: perspective(400px) rotate3d(0, 1, 0, 90deg);
    animation-timing-function: ease-in;
    opacity: 0;
  }

  40% {
    transform: perspective(400px) rotate3d(0, 1, 0, -20deg);
    animation-timing-function: ease-in;
  }

  60% {
    transform: perspective(400px) rotate3d(0, 1, 0, 10deg);
    opacity: 1;
  }

  80% {
    transform: perspective(400px) rotate3d(0, 1, 0, -5deg);
  }

  to {
    transform: perspective(400px);
  }
}
.fadeout {
  visibility: hidden;
  opacity: 0;
  transition: visibility 0s 1s, opacity 1s linear;
}

[draggable] {
  user-select: none;
}

/////////////////////////////// Other CSS /////////////////////////////////////

.score-box {
  position: fixed;
  top: 0px;
  right: 0px;
  z-index: 6000;
  text-shadow: #fff 1px 1px, #fff -1px 1px, #fff -1px -1px, #fff 1px -1px;
}
.coin-box {
  position: fixed;
  top: 0px;
  left: 0px;
  z-index: 6000;
  text-shadow: #fff 1px 1px, #fff -1px 1px, #fff -1px -1px, #fff 1px -1px;
}
.score-text {
  font-size: 2em;
}
.score-text i {
  font-size: 0.7em;
  line-height: 0.5em;
  margin-top: -7px;
}
.score {
  font-size: 2.1rem;
  text-align: center;
  font-weight: bold;
  background-position: center 50px;
  background-repeat: no-repeat;
  background-size: 35%;
  width: 100px;
  padding-top: 10px;
}
.map {
  width: 100%; 
  height: 100%; 
}
.enable-geolocation {
  padding: 1rem;
}
.big-avatar, .mid-avatar {
  border-radius: 50%;
  border: 5px solid #f00;
}
.big-avatar, .mid-avatar {
  overflow: hidden;
  position: relative;
  display: inline-block;
  vertical-align: top;
}
.big-avatar div, .mid-avatar div {
  border-radius: 50%;
}
.big-avatar div, .mid-avatar div {
  vertical-align: top;
}
.big-avatar div {
  background-size: 68px 68px;
  width: 68px;
  height: 68px;
}
.mid-avatar div {
  background-size: 34px 34px;
  width: 34px;
  height: 34px;
}
.big-avatar span, .mid-avatar span, .mid-avatar label, .big-avatar label {
  background: rgba(0,0,0,.54);
  bottom: 0;
  color: #fff;
  font-size: 9px;
  font-weight: bold;
  left: 0;
  line-height: 9px;
  position: absolute;
  padding: 7px 0;
  text-align: center;
  width: 70px;
  display: block;
  cursor: pointer;
}
.bottom-dark-banner {
  background-color: rgba(0, 0, 0, 0.5);
  position: fixed;
  bottom: 0px;
  width: 100%;
  color: #fff;
  padding: 10px;
}
.bottom-dark-banner .title {
  font-size: 1.5em;
}
.q-pb-xxl {
  padding-bottom: 120px;
}
.q-pb-xxxl {
  padding-bottom: 200px;
}
.q-carousel-quick-nav {
  background: transparent;
}
.q-carousel-quick-nav>.text-primary {
  color: white !important;
}
.q-carousel .q-btn.disabled {
  display: none;
}
.list-group { 
  list-style-type: none; 
  padding: 0; 
  margin: 0; 
}
.list-group-item { 
  display: flex; 
  flex-flow: row nowrap; 
  align-items: center; 
  padding: 0rem 0.2rem 0.4rem 0; 
}
.list-group-item p { 
  flex-grow: 1; 
  padding: 0; 
  margin: 0; 
  overflow-x: hidden; 
}
.list-group-item .handle { 
  font-size: 2rem; 
  padding-right: 0.3rem; 
}
.list-group-item .q-btn { 
  width: 3rem
}
.inventory-items div { 
  float: left; 
  margin-right: 15px; 
  width: 100px; 
  height: 175px; 
  overflow: hidden; 
  text-align: center;
}
.inventory-items div img { 
  width: 100px; 
  height: 100px;
}
.menu .q-btn .q-icon {
  font-size: 2.4em;
}
.objects-list img {
  border: 1px solid #fff;
}
.selected {
  background: #eee;
  border: 1px solid #ccc !important;
}
.q-list {
  border: 0px !important;
}
.q-list-separator {
  margin-top: 10px;
  border: 1px solid #e0e0e0 !important;
}
.location-gps p {
  color: rgba(0,0,0,0.46);
}
.location-gps-inputs {
  display: flex;
  flex-flow: row nowrap;
}
.location-gps-inputs input {
  display: block;
  margin: 0.5rem;
  border: 0px none;
  border-bottom: 1px solid #ccc;
  width: 8rem;
}
.location-gps-inputs label {
  display: block;
  margin: 0.5rem;
  color: rgba(0,0,0,0.46);
}
.image-and-text-aligned {
  vertical-align: top;
}
.story .bubble-top, .story .bubble-bottom {
  font-size: 0px;
}
.story .bubble-middle {
  width: 100%;
  padding: 0 15px;
  background-position: top left !important;
  background-size: 100% !important;
  max-height: 160px;
  overflow-y: auto;
}
.story .bubble-top img, .story .bubble-bottom img {
  width: 100%;
  padding: 0;
  margin: 0;
}
.story .character img {
  width: 100%
}
#q-app .bg-facebook {
  background: #4267B2;
}
#q-app .bg-google {
  background: #fff;
}
#q-app .bg-google .q-btn-inner {
  color: #7D7D7D;
}
#q-app .fa-google:before {
  color: transparent;
  background-image: url('/statics/icons/social-networks/g-logo.png');
  background-position: 0px;
  background-repeat: no-repeat;
  background-size: 100%;
}
.score-bar {
  text-align: center;
  font-size: 1.2em;
}
.score-bar img {
  height: 20px;
}
.q-list .score {
  font-size: 1rem;
  background-position: right 10px;
  background-size: 16px;
  width: 60px;
  padding-right: 18px;
}

/////////////////////////////////////////////////////////// OLD CSS ///////////////////////////////////////////////////////////////////// 
// app global css

/*

#main-view {
  padding: 0;
}

.subtitle {
  font-size: 0.9rem;
  color: $dark-grey-text;
  line-height: 0.9rem;
  margin-bottom: 0.5rem;
}

.quest-avatar {
  width: 200px;
  border-radius: 50%
}
.q-list.challenges .score {
  font-weight: normal;
  text-align: right;
  background-position: right 7px;
}

.tab-content, .page-content {
  padding: 10px;
}
.tab-content h2 {
  font-size: 1rem;
}
.top-padding-middle {
  padding-top: 25px;
}
.padding-medium {
  padding: 10px;
}
.bottom-separator {
  border-bottom: 1px solid #ffc90e;
}

.small-icon {
  width: 40px
}

.search {
  position: absolute;
  top: 0px;
  left: 0px;
  right: 0px;
  background: #fff;
  z-index: 2900;
}

// Team specific CSS

.link-below-button { 
  width: 100%;
  padding-top: 1rem;
  text-align: center;
}

.challenge-progression {
  width: 90px;
  height: 26px;
  border: 1px solid #ffc90e;
  text-align: center;
  font-size: 14px;
  background-image: url('/statics/images/background-yellow.png');
  background-repeat: no-repeat;
  padding: 3px;
}

*/
