// We do not use firebase since we are now using Sentry to monitor the application errors
// Firebase is also not used for connection yet, si there is no point to keeep this for now 
// AR 08/10/2020


/*import * as firebase from "firebase/app";

import "firebase/performance";

var firebaseConfig = require('./../../config/firebase.json');

// Initialize Firebase
firebase.initializeApp(firebaseConfig);

// Initialize Performance Monitoring and get a reference to the service
firebase.performance();*/